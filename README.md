# 🏛 CorpusVisionBot

Telegram-бот для распознавания корпусов зданий по фотографиям с помощью нейронной сети на базе **MobileNetV2**.

Пользователь отправляет фото здания — бот определяет номер корпуса, степень уверенности и адрес.

---

## 📋 Возможности

- 📸 **Классификация изображений** — определение корпуса здания по фотографии
- 🏠 **Адрес корпуса** — бот возвращает адрес определённого здания
- 📊 **Уверенность** — отображение процента уверенности модели в предсказании
- 🧠 **MobileNetV2** — лёгкая и быстрая нейросеть (transfer learning)
- 💾 **SQLite** — хранение информации о пользователях
- ⚡ **Асинхронность** — бот работает на `aiogram 3` и `aiosqlite`

---

## 🏗 Структура проекта

```
CorpusVisionBot/
├── main.py                          # Точка входа
├── requirements.txt                 # Зависимости
├── LICENSE                          # MIT License
├── README.md
│
├── data/
│   ├── db/
│   │   └── users.db                 # БД пользователей (создаётся автоматически)
│   └── downloads/                   # Загруженные фото пользователей
│
├── dataset/                         # Датасет для обучения (не входит в репозиторий)
│   ├── building_01/
│   ├── building_02/
│   ├── ...
│   └── other/
│
├── neural_network/
│   ├── train.py                     # Скрипт обучения модели
│   ├── predict.py                   # Скрипт предсказания класса
│   └── best_model.keras             # Обученная модель
│
└── src/
    ├── configs/
    │   └── config.py                # Конфигурация проекта (пути, параметры модели)
    ├── db/
    │   ├── save_user_info.py        # Сохранение данных пользователя
    │   └── get_user_info.py         # Получение данных пользователя
    ├── handlers/
    │   ├── __init__.py              # Регистрация роутеров
    │   ├── cmds/
    │   │   └── start_cmd.py         # Обработчик команды /start
    │   ├── media/
    │   │   └── media_handler.py     # Обработчик фотографий
    │   └── text/
    │       └── text_handler.py      # Обработчик текстовых сообщений
    ├── resources/
    │   ├── messages.py              # Текстовые сообщения бота
    │   └── errors.py                # Текстовые сообщения об ошибках
    └── utils/
        ├── setup_project_env.py     # Инициализация окружения проекта
        └── setup_user_env.py        # Инициализация окружения пользователя
```

---

## ⚙️ Установка и запуск

### 1. Клонировать репозиторий

```bash
git clone https://github.com/pygradsky/CorpusVisionBot.git
cd CorpusVisionBot
```

### 2. Создать виртуальное окружение

```bash
python -m venv venv
# Windows
venv\Scripts\activate
# Linux / macOS
source venv/bin/activate
```

### 3. Установить зависимости

```bash
pip install -r requirements.txt
```

### 4. Создать файл `.env`

В корне проекта создайте файл `.env`:

```
BOT_TOKEN=ваш_токен_от_@BotFather
```

### 5. Запустить бота

```bash
python main.py
```

---

## 🧠 Обучение модели

1. Подготовьте датасет: папка `dataset/` должна содержать подпапки с классами (`building_01`, `building_02`, ..., `other`), в каждой — изображения соответствующего корпуса.

2. Запустите обучение:

```bash
python -m neural_network.train
```

3. Обученная модель сохранится в `neural_network/new_model.keras`. Переименуйте её в `best_model.keras` для использования ботом.

---

## 🔧 Конфигурация

Основные параметры находятся в `src/configs/config.py`:

| Параметр         | Значение          | Описание                          |
|------------------|-------------------|-----------------------------------|
| `IMG_SIZE`       | `(224, 224)`      | Размер входного изображения       |
| `BATCH_SIZE`     | `32`              | Размер батча при обучении         |
| `EPOCHS`         | `20`              | Количество эпох обучения          |
| `NN_FILE_PATH`   | `best_model.keras`| Путь к обученной модели           |

---

## 🏠 Поддерживаемые корпуса

| Класс          | Адрес                                             |
|----------------|---------------------------------------------------|
| `building_01`  | 127550 г. Москва, Лиственничная аллея, д.4А       |
| `building_02`  | 127550 г. Москва, Лиственничная аллея, д. 4       |
| `building_03`  | 127550 г. Москва, Лиственничная аллея, д.3        |
| `building_04`  | 127550 г. Москва, ул. Пасечная, д. 2              |
| `building_05`  | 127550 г. Москва, ул. Тимирязевская, д. 48        |
| `building_07`  | неизвестен                                         |
| `building_08`  | 127550 г. Москва, Тимирязевская улица, д. 47      |
| `building_09`  | 127550 г. Москва, ул. Тимирязевская, д. 52        |
| `building_10`  | 127550 г. Москва, Тимирязевская улица, д. 49      |
| `other`        | Изображение не является корпусом                   |

---

## 📦 Технологии

- **Python 3.13+**
- **aiogram 3** — асинхронный фреймворк для Telegram Bot API
- **TensorFlow / Keras** — нейронная сеть (MobileNetV2)
- **aiosqlite** — асинхронная работа с SQLite
- **python-dotenv** — загрузка переменных окружения

---

## 📄 Лицензия

Проект распространяется под лицензией [MIT](LICENSE).

© 2026 Fedor

